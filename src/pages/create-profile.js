import React from 'react';
import ProfileForm from '../components/profileForm';

const CreateProfile = () => {
    const aVariable = '';

    return (
        <div>
            <ProfileForm />
        </div>
    );
};

export default CreateProfile;
